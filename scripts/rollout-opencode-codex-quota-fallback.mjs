#!/usr/bin/env node
/**
 * Same as rollout-codex-agent-presets.mjs — managed agents → OpenCode + default free preset (quota / cost path).
 * Kept as a second entrypoint for operator docs that mention "Codex quota fallback".
 */

import "./rollout-codex-agent-presets.mjs";
