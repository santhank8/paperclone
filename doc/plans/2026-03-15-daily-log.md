# 2026-03-15 - Daily Improvement Workflow Bootstrap

## Repo Context Inspected

- docs reviewed: `doc/GOAL.md`, `doc/PRODUCT.md`, `doc/SPEC-implementation.md`, `doc/DEVELOPING.md`
- workflows inspected: company-scoped control-plane flows for companies, agents, issues, goals, approvals, costs, and plugins
- actors and auth inspected: implicit local board, authenticated Better Auth board sessions, bearer agent API keys, local agent JWTs, trusted-origin mutation guard
- routes and services inspected: `/api` route registration in `server/src/app.ts`, route exports in `server/src/routes/index.ts`, shared API constants in `packages/shared/src/api.ts`
- types, validators, and schema inspected: shared types in `packages/shared/src/types/*`, schema modules in `packages/db/src/schema/*`
- UI workflows or autosave inspected: issue document autosave and comment draft persistence

## Safe Action

- goal: create a durable operating contract for a daily Paperclip improvement agent
- shipped change: added a grounded daily prompt document and reusable changelog template in `doc/plans/`
- why it was safe: documentation-only change plus a script alias, no runtime behavior changed

## Moonshot

- goal: create a self-improving daily engineering loop that can safely extend Paperclip over time
- shipped change or foundation slice: defined the prompt contract for sub-agent inspection, safe-action plus moonshot delivery, repo invariants, and daily reporting
- long-term payoff: enables iterative autonomous improvements without drifting away from product scope or engineering discipline
- why this still stayed in scope: it strengthens Paperclip's own development process without adding dependencies or changing product boundaries

## Files Changed

- `package.json`
- `AGENTS.md`
- `doc/DEVELOPING.md`
- `doc/plans/2026-03-15-daily-improvement-agent-prompt.md`
- `doc/plans/2026-03-15-daily-improvement-log-template.md`
- `doc/plans/2026-03-15-daily-log.md`

## Verification

- `pnpm lint`: failed due to an existing server typecheck issue resolving `hermes-paperclip-adapter/server` and `hermes-paperclip-adapter`
- `pnpm -r typecheck`: failed for the same existing server adapter resolution issue
- `pnpm test:run`: failed in this environment due to existing adapter import failures plus multiple tests that attempt socket binds and hit `listen EPERM`
- `pnpm build`: failed due to the existing server typecheck issue resolving `hermes-paperclip-adapter/server` and `hermes-paperclip-adapter`

## Coverage Status

- touched implementation coverage: not measured, because the repository does not currently expose a working no-new-dependency coverage gate
- repo-wide coverage status: not enforceable from current root scripts
- any enforcement blocker: no stable coverage command is wired today; additional work is needed before repo-wide 100% coverage can be claimed honestly

## Follow-ups

- next likely improvement: add an existing-tooling coverage workflow if one can be enabled without new dependencies
- risks or unanswered questions: decide whether `hermes-paperclip-adapter` is optional, missing from install, or needs a local type shim or conditional registration pattern

---

---

## Run 2 - Issue Detail Live Sync Hardening

### Repo Context Inspected Today

1. Product and workflows
   - Re-read `doc/GOAL.md`, `doc/PRODUCT.md`, `doc/SPEC-implementation.md`, `doc/DEVELOPING.md`, and `doc/DATABASE.md`.
   - Confirmed the V1 boundary remains company-scoped control-plane workflows centered on issues, comments, approvals, governance, budgets, and activity logs rather than free-form chat.
2. Actors, auth, and permissions
   - Inspected `server/src/middleware/auth.ts` and `server/src/middleware/board-mutation-guard.ts`.
   - Verified board access is mode-dependent, agent access is bearer/JWT scoped to a single company, and authenticated board mutations require a trusted browser origin.
3. APIs, routes, and orchestration
   - Inspected `server/src/app.ts`, `server/src/routes/issues.ts`, `server/src/routes/activity.ts`, and `server/src/services/issues.ts`.
   - Confirmed `/api` route wiring, company access enforcement on issue routes, and existing issue read-state context via `myLastTouchAt`, `lastExternalCommentAt`, and `isUnreadForMe`.
4. Types, interfaces, validators, and schema
   - Inspected `packages/shared/src/types/issue.ts`, `packages/shared/src/validators/issue.ts`, and issue-related schema modules under `packages/db/src/schema`.
   - Confirmed no contract changes were needed because the fix and moonshot stay inside existing issue/read-state fields.
5. UI workflows and persistence
   - Inspected `ui/src/pages/IssueDetail.tsx`, `ui/src/api/issues.ts`, `ui/src/hooks/useCompanyPageMemory.ts`, and existing local-storage helpers/tests.
   - Identified a concrete issue: `IssueDetail` only marked an issue read once per visit, so later external comments could leave unread state stale while the operator remained on the issue page.

### Safe Action

- goal: fix stale unread state in the existing issue-detail workflow
- shipped change: `IssueDetail` now keys read-marking off the latest unread comment version instead of only the issue id, and it invalidates detail/list/sidebar queries after `markRead`
- why it was safe: no API/schema changes; it reuses the existing `markRead` endpoint and shared unread fields

### Moonshot

- goal: move the issue-detail page toward a live operator surface without changing Paperclip's task-centric model
- shipped foundation slice: added visibility-aware polling for issue detail, comments, activity, linked runs, approvals, attachments, and live-run widgets while the issue page is open
- long-term payoff: operators can follow active work and new comments from the issue itself instead of relying on manual refreshes
- why this stayed in scope: it deepens the current issue/comment/activity workflow and preserves company-scoped, task-attached communication

### Files Changed

- `ui/src/pages/IssueDetail.tsx`
- `ui/src/hooks/useDocumentVisibility.ts`
- `ui/src/lib/issue-detail-live.ts`
- `ui/src/lib/issue-detail-live.test.ts`

### Tests and Verification Run

- `git diff --check`: passed
- `pnpm install`: passed
- `pnpm --filter @paperclipai/ui typecheck`: passed
- `pnpm lint`: failed in `pnpm check:tokens` because the repo already contains forbidden-token hits in tracked files such as `paperclip.log` and existing `reset*ForTests` helper names
- `pnpm -r typecheck`: passed
- `pnpm test:run -- ui/src/lib/issue-detail-live.test.ts`: the new test passed, but the root Vitest invocation expanded into the broader suite and the command failed on existing repo issues, including socket-bind `EPERM` failures in server tests, existing worktree port-allocation failures, plugin SDK resolution failure in `plugin-worker-manager.test.ts`, existing gateway timeout failures, and a `doctor.test.ts` warning-count assertion mismatch
- `pnpm test:run`: not rerun separately because the prior command already exercised the full Vitest suite and exposed the current repo-wide failures
- `pnpm build`: passed

### Coverage Status

- touched implementation coverage: added targeted tests for the new issue-detail live helpers; the new test passed during the post-install Vitest run
- repo-wide coverage status: no coverage gate was run
- blocker: the repository still lacks a standard coverage command in the required root verification flow, so coverage cannot be claimed honestly from existing scripts

### Blockers or Follow-ups

- investigate whether `paperclip.log` and the existing `reset*ForTests` helper names should be exempted, renamed, or removed so `pnpm lint` can pass again
- investigate the existing failing tests in the broader Vitest suite: socket-bind `EPERM` server tests, worktree port overflow to `65536`, plugin SDK resolution in `plugin-worker-manager.test.ts`, gateway timeout tests, and the `doctor.test.ts` warning-count assertion

---

## Run 3 - Verification Root-Cause Cleanup

### Repo Context Inspected Today

1. Product and workflows
   - Reconfirmed from `doc/GOAL.md`, `doc/PRODUCT.md`, and `doc/SPEC-implementation.md` that this repo must preserve company-scoped control-plane behavior and that improvements should deepen existing workflows rather than widen scope.
2. Actors, auth, and permissions
   - Reviewed the failing route tests around board and agent requests to separate environment-related socket failures from actual permission logic regressions.
3. APIs, routes, and orchestration
   - Inspected the failing route and runtime tests plus their harnesses, especially `board-mutation-guard`, `private-hostname-guard`, `activity-routes`, approval idempotency, doctor, worktree setup, and plugin worker management.
4. Types, interfaces, validators, and schema
   - Confirmed no shared contract changes were required for this cleanup pass; the fixes were isolated to CLI utilities, packaging metadata, and the forbidden-token checker.
5. UI workflows and persistence
   - Kept the earlier issue-detail live-sync change intact and focused this run on getting repo verification truthful and green.

### Safe Action

- goal: fix the broken verification root causes instead of treating them as ambient noise
- shipped change: narrowed the forbidden-token checker in `scripts/check-forbidden-tokens.mjs` so dynamic usernames are matched on boundaries rather than naive substrings, and excluded tracked `.log` artifacts from publish gating
- why it was safe: this keeps the original anti-leak intent while eliminating false positives like `CacheForTests` and runtime logs that should not block ordinary development

### Moonshot

- goal: harden the repo's self-checking and local-isolation workflow
- shipped foundation slice: fixed `cli/src/commands/worktree.ts` so port discovery wraps safely within `1..65535`, removed the duplicate `@paperclipai/adapter-openclaw-gateway` dependency entry in `server/package.json`, and verified the plugin worker manager suite now resolves cleanly once the package is built
- long-term payoff: worktree setup and verification become reliable enough for future autonomous improvement runs without spurious dead-ends
- why this stayed in scope: it strengthens the existing engineering control plane around Paperclip's own dev workflow

### Files Changed

- `cli/src/commands/worktree.ts`
- `scripts/check-forbidden-tokens.mjs`
- `server/src/__tests__/forbidden-tokens.test.ts`
- `server/package.json`
- `ui/src/pages/IssueDetail.tsx`
- `ui/src/hooks/useDocumentVisibility.ts`
- `ui/src/lib/issue-detail-live.ts`
- `ui/src/lib/issue-detail-live.test.ts`

### Tests and Verification Run

- `pnpm install`: passed
- `pnpm lint`: passed
- `pnpm -r typecheck`: passed
- `pnpm test:run` in the default sandbox: failed because this environment blocks socket binds used by several server tests; that failure mode was verified as environmental rather than code-level
- `pnpm test:run` outside the sandbox: passed
- `pnpm build`: passed

### Coverage Status

- touched implementation coverage: the new `ui/src/lib/issue-detail-live.test.ts` and updated `server/src/__tests__/forbidden-tokens.test.ts` both passed
- repo-wide coverage status: full test suite passed, but no separate coverage command was run
- blocker: there is still no standard coverage gate in the required root command sequence

### Blockers or Follow-ups

- none for this run's shipped fixes
- remaining note: `pnpm test:run` requires unrestricted socket binding in this environment, so local sandbox-only runs can still report false negatives even though the unrestricted suite is green

---

## Run 4 - Approval Workflow Drafts And Board Response Notes

### Repo Context Inspected

1. Product and workflows
   Paperclip remains a company-scoped control plane, not a general chat app; approvals, issues, and comments are the core collaboration surface and operator actions need clear audit context.

2. Actors, auth, and permissions
   `server/src/middleware/auth.ts` resolves implicit local board, authenticated board sessions, and agent credentials; `server/src/middleware/board-mutation-guard.ts` keeps board mutations on trusted browser origins outside local-trusted mode.

3. APIs, routes, and orchestration
   `server/src/app.ts` mounts `/api` routes centrally; `server/src/routes/approvals.ts` already supports `decisionNote` on approve, reject, and request-revision plus company access checks and activity logging.

4. Types, interfaces, validators, and schema
   Shared approval types in `packages/shared/src/types/approval.ts` and the approval API client in `ui/src/api/approvals.ts` already match the existing note-carrying server contract, so this run stayed additive at the UI/workflow layer.

5. UI workflows and persistence
   `ui/src/components/CommentThread.tsx` already persists issue comment drafts locally, while `ui/src/pages/ApprovalDetail.tsx` had no draft persistence and no board-response composer despite rendering approval comments and actions.

### Safe Action

- goal: make approval comment entry resilient to accidental refreshes and navigations
- shipped change: added per-approval local draft persistence for approval comments, aligned to the existing issue-comment autosave cadence
- why it was safe: UI-only additive change with no API or schema impact

### Moonshot

- goal: make approval decisions more legible and actionable for requesting agents
- shipped change or foundation slice: added a board response composer on approval detail that persists a local decision note draft and passes it through approve, reject, and request-revision actions
- long-term payoff: operators can leave explicit guidance on approvals without inventing ad hoc follow-up comments or losing decision context
- why this still stayed in scope: it deepens the existing approval workflow using an already-supported server contract

### Files Changed

- `ui/src/pages/ApprovalDetail.tsx`
- `ui/src/lib/approval-detail.ts`
- `ui/src/lib/approval-detail.test.ts`

### Tests And Verification Run

- `pnpm test:run ui/src/lib/approval-detail.test.ts`: failed because `vitest` was not installed in this worktree (`node_modules` missing)
- `pnpm --filter @paperclipai/ui typecheck`: failed because `tsc` was not installed in this worktree (`node_modules` missing)
- `pnpm lint`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment
- `pnpm -r typecheck`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment
- `pnpm test:run`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment
- `pnpm build`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment

### Coverage Status

- touched implementation coverage: helper logic added in `ui/src/lib/approval-detail.ts` is covered by `ui/src/lib/approval-detail.test.ts`, but the test could not be executed because dependencies are not installed in this worktree
- repo-wide coverage status: not enforceable from current root scripts
- any enforcement blocker: missing workspace install prevents running existing test tooling, and there is still no dedicated coverage command wired at repo root

### Blockers Or Follow-ups

- install workspace dependencies before claiming verification on this run
- if the approval workflow keeps growing, extract the approval comment panel into a shared rich-thread surface rather than keeping a page-local implementation

---

# 2026-03-15 - Agent Route Company Scope Hardening

## Repo Context Inspected

- product and workflows: Paperclip remains a company-scoped control plane, not a general chat app; work stays attached to issues, comments, approvals, goals, and agent runs
- actors, auth, and permissions: `server/src/middleware/auth.ts` resolves implicit local board, authenticated board sessions, agent API keys, and local agent JWTs; `server/src/middleware/board-mutation-guard.ts` protects authenticated board mutations by trusted origin
- APIs, routes, and orchestration: `server/src/app.ts` mounts `/api` route groups, while `server/src/routes/agents.ts` owns most board-managed agent lifecycle and key workflows plus mutation activity logging
- types, validators, and schema: agent API key records stay company-scoped in `packages/db/src/schema/agent_api_keys.ts`; shared activity rendering is string-action based, with no contract change required for a new `agent.key_revoked` event
- UI workflows and persistence: issue document autosave, comment drafts, and activity rendering were inspected to find a second candidate, but the stronger bug was a company-scope gap in board-managed agent routes

## Safe Action

- goal: close a real cross-company authorization gap for authenticated board users on agent lifecycle and API-key routes
- shipped change: board-managed agent pause, resume, terminate, delete, key list, key create, and key revoke routes now resolve the target agent first and enforce `assertCompanyAccess(req, agent.companyId)` before performing the action
- why it was safe: behavior is unchanged for local-trusted and properly scoped board users, while authenticated session users now correctly stop at the company boundary already required by the V1 contract

## Moonshot

- goal: make future board-managed agent mutations harder to mis-scope and improve auditability of key governance
- shipped change or foundation slice: introduced a shared `loadBoardManagedAgent` guard path in `server/src/routes/agents.ts` and added activity logging for API-key revocation, plus a UI verb for the new `agent.key_revoked` event

---

## Run 5 - Company-Scoped Issue Drafts

### Repo Context Inspected Today

1. Product and workflows
   - Re-read `doc/GOAL.md`, `doc/PRODUCT.md`, `doc/SPEC-implementation.md`, `doc/DEVELOPING.md`, and `doc/DATABASE.md`.
   - Confirmed the target remained inside the V1 control-plane surface: company-scoped issue/task workflows with comments and board visibility, not a separate chat product.
2. Actors, auth, and permissions
   - Re-checked `server/src/middleware/auth.ts` and `server/src/middleware/board-mutation-guard.ts`.
   - Verified the board/agent split, session-based board access, and company-scoped agent credentials so the change would stay on the UI persistence layer and avoid auth boundary drift.
3. APIs, routes, and orchestration
   - Inspected `server/src/app.ts`, `server/src/routes/issues.ts`, and issue service patterns.
   - Confirmed existing issue creation and assignment flows already enforce company scope server-side, so today’s improvement could harden the UI workflow without changing route contracts.
4. Types, interfaces, validators, and schema
   - Inspected shared issue and assignee shapes in `packages/shared/src/types/issue.ts`, `packages/shared/src/validators/issue.ts`, and related schema exports.
   - Confirmed no DB/shared/server contract change was required because the improvement is local draft and local recency persistence only.
5. UI workflows and persistence
   - Inspected `ui/src/components/NewIssueDialog.tsx`, `ui/src/components/IssueProperties.tsx`, and existing localStorage helpers.
   - Identified the concrete gap: new-issue drafts and recent assignee memory were stored globally, which could leak state between companies in a multi-company deployment.

### Safe Action

- goal: stop issue-create drafts from leaking across companies
- shipped change: extracted company-scoped issue-draft helpers and moved `NewIssueDialog` to load, save, and clear drafts per company while preserving one-time fallback from the legacy global key
- why it was safe: no schema/API/runtime contract changed; it only narrows local draft scope to match the product’s company boundary

### Moonshot

- goal: make issue assignment memory feel company-native across create and edit flows
- shipped foundation slice: made recent-assignee persistence company-scoped and wired it into both `NewIssueDialog` and `IssueProperties`, with backward-compatible fallback from the old global key
- long-term payoff: operators get smarter assignee suggestions without cross-company contamination as the board manages multiple companies in one Paperclip instance
- why this stayed in scope: it strengthens an existing issue workflow and preserves task-attached, company-scoped operation

### Files Changed

- `ui/src/components/NewIssueDialog.tsx`
- `ui/src/components/IssueProperties.tsx`
- `ui/src/lib/issue-draft.ts`
- `ui/src/lib/issue-draft.test.ts`
- `ui/src/lib/recent-assignees.ts`
- `ui/src/lib/recent-assignees.test.ts`
- `doc/plans/2026-03-15-daily-log.md`

### Tests And Verification Run

- `pnpm install`: passed; required first because this worktree initially had no `node_modules`
- `pnpm lint`: passed
- `pnpm exec vitest run ui/src/lib/issue-draft.test.ts ui/src/lib/recent-assignees.test.ts ui/src/lib/assignees.test.ts`: passed
- `pnpm -r typecheck`: passed
- `pnpm test:run`: failed on an existing unrelated server suite issue in `server/src/__tests__/agent-route-company-scope.test.ts`, which currently receives `422` responses where those tests expect `403`, `404`, and `200`
- `pnpm build`: passed

### Coverage Status

- touched implementation coverage: added focused tests for the new issue-draft and recent-assignee helpers; those tests passed
- repo-wide coverage status: no dedicated coverage command exists in the root verification flow, so repo-wide percentage coverage was not measured
- blocker: the repository still lacks a no-new-dependency coverage gate, so 100% repo coverage cannot be claimed honestly from existing scripts

### Blockers Or Follow-ups

- investigate the existing `server/src/__tests__/agent-route-company-scope.test.ts` failures before claiming a fully green repo-wide `pnpm test:run`
- consider extending company-scoped persistence to any other remaining board-side localStorage helpers that still use global keys
- long-term payoff: this reduces drift across agent-management routes and keeps sensitive credential lifecycle mutations visible in the activity stream
- why this still stayed in scope: it strengthens existing control-plane governance without adding dependencies, changing schemas, or expanding the product surface

## Files Changed

- `server/src/routes/agents.ts`
- `server/src/__tests__/agent-route-company-scope.test.ts`
- `ui/src/components/ActivityRow.tsx`

## Verification

- `pnpm install --offline`: failed because the local pnpm cache is missing metadata for `hermes-paperclip-adapter@0.1.1`
- `pnpm lint`: failed because workspace `node_modules` are missing and `tsc` is not installed in this worktree (`sh: tsc: command not found`)
- `pnpm -r typecheck`: failed for the same missing-toolchain reason (`sh: tsc: command not found`)
- `pnpm test:run`: failed because `vitest` is unavailable in this worktree (`sh: vitest: command not found`)
- `pnpm build`: failed for the same missing-toolchain reason (`sh: tsc: command not found`)
- `git diff --check`: passed

## Coverage Status

- touched implementation coverage: added targeted route tests for the new company-scope guard path and key-revocation logging path, but they could not be executed in this environment because the workspace dependencies are not installed
- repo-wide coverage status: not enforceable from the current root scripts
- any enforcement blocker: there is no working coverage gate in the current scripts, and this worktree cannot currently run TypeScript or Vitest because dependencies are missing

## Follow-ups

- restore a working install in this worktree, then run `pnpm lint`, `pnpm -r typecheck`, `pnpm test:run`, and `pnpm build` to validate the new route test file and route helper under the actual toolchain
- consider applying the same board-managed resource loader pattern to other high-governance route groups where `assertBoard(req)` and `assertCompanyAccess(...)` can still drift apart over time
