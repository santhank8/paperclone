---
phase: 01-compose-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Human confirms the stack starts with a single command on docker-001"
    - "Human confirms server container reaches healthy state"
    - "Human confirms db port is not reachable from outside Docker network"
    - "Human confirms restart behavior works after docker kill"
  artifacts:
    - path: "docker/.env"
      provides: "Operator-created .env file from template (not committed)"
      contains: "BETTER_AUTH_SECRET"
  key_links:
    - from: "docker/.env"
      to: "docker/docker-compose.yml"
      via: "Compose auto-loads .env from compose file directory"
      pattern: "BETTER_AUTH_SECRET"
---

<objective>
Verify the hardened stack works on docker-001: services start, server reaches healthy, both services restart after kill, and port 5432 is not exposed externally.

Purpose: The previous plans edited files locally. This checkpoint confirms the changes actually work in the target environment (docker-001 with NFS bind mounts).
Output: Confirmation that Phase 1 success criteria are met, or a list of issues to resolve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-compose-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Prepare docker-001 environment</name>
  <files>docker/.env</files>
  <action>
Human performs these steps on docker-001 before docker compose up:

1. Create bind mount directories (NFS-backed at /mnt/paperclip):
   sudo mkdir -p /mnt/paperclip/pgdata /mnt/paperclip/data
   sudo chown -R $(id -u):$(id -g) /mnt/paperclip

2. Find UID/GID for .env:
   id
   Note uid=XXXX and gid=XXXX

3. Copy and fill in .env:
   cd /path/to/paperclip/docker
   cp .env.template .env
   Edit .env and set:
   - BETTER_AUTH_SECRET — generate with: openssl rand -hex 32
   - POSTGRES_PASSWORD — generate with: openssl rand -hex 16
   - PAPERCLIP_PUBLIC_URL — set to http://[docker-001-ip]:3100 (actual LAN IP)
   - USER_UID — uid from the id command
   - USER_GID — gid from the id command
   - Leave POSTGRES_USER=paperclip and POSTGRES_DB=paperclip as-is

4. Verify .env is complete:
   docker compose config
   Should print resolved compose without errors.
   If any :? guard fires ("variable is not set"), fix that variable.
  </action>
  <verify>docker compose config (exits 0 with no "variable is not set" errors)</verify>
  <done>docker/.env exists with all required values, docker compose config runs without errors</done>
  <resume-signal>Type "ready" when .env is filled in and `docker compose config` runs without errors</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Start stack and verify all Phase 1 success criteria</name>
  <files></files>
  <action>
Run these 6 checks on docker-001. All must pass.

Check 1 — Start with single command:
  cd /path/to/paperclip/docker
  docker compose up -d
  Expected: no errors, two services created/started

Check 2 — Server reaches healthy (wait up to 90 seconds):
  watch -n5 docker ps
  Expected: server container STATUS shows "(healthy)" not "(health: starting)"
  This confirms HEALTHCHECK is wired and /health is responding

Check 3 — Restart after kill (crash recovery):
  docker kill $(docker compose ps -q server)
  # wait 10-15 seconds
  docker ps
  Expected: server container is back up automatically

Check 4 — PostgreSQL port not exposed:
  nc -zv localhost 5432
  Expected: "Connection refused" — port NOT bound to host
  If "succeeded": port still exposed — check compose file

Check 5 — Health endpoint responds:
  curl -s http://localhost:3100/health
  Expected: {"status":"ok",...} — HTTP 200

Check 6 — Bind mounts have data:
  ls /mnt/paperclip/pgdata   # expect PG_VERSION, base/, global/, etc.
  ls /mnt/paperclip/data     # expect Paperclip app directory structure
  </action>
  <verify>All 6 checks pass as described above</verify>
  <done>Human confirms all 6 checks pass. Phase 1 success criteria satisfied.</done>

  <what-built>
    Plans 01-01 and 01-02 produced:
    - docker/docker-compose.yml: restart policies, bind mounts, parameterized creds, no DB port exposure, PAPERCLIP_PUBLIC_URL guard
    - Dockerfile: HEALTHCHECK targeting /health with 60s start-period
    - docker/.env.template: all variables documented
  </what-built>

  <resume-signal>
    Type "approved" if all 6 checks pass.
    Describe which checks failed if any did not pass (e.g., "Check 4 failed — port still exposed").
  </resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria must be confirmed by the human:
1. `docker compose up -d` starts both services without errors
2. Server container STATUS shows `(healthy)` in `docker ps`
3. Server restarts after `docker kill` without manual intervention
4. `nc -zv localhost 5432` returns "Connection refused"
5. `docker/.env.template` exists (already verifiable by file existence)
</verification>

<success_criteria>
Human confirms all 6 verification checks passed on docker-001.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compose-foundation/01-03-SUMMARY.md` summarizing:
- Which checks passed
- Any issues encountered and how they were resolved
- Final state of the stack (running services, bind mount paths confirmed)
- UID/GID values used (so Phase 2 has this context)
</output>
