# Session Handoff
**Created:** 2026-03-15T20:10:00Z
**Branch:** master
**Project:** paperclip (AI Skills Lab)

## What Was Happening
Massive session: wiped Paperclip DB, rebuilt AI Skills Lab from scratch with one-agent-at-a-time methodology. Built 4 agents (Research, SkillBuilder, QC, CustomerRelations), established autonomous Research→SkillBuilder→QC pipeline, built CLI installer, published to npm, set up YouTube channel assets, installed Remotion + html2pptx tooling.

## Current State
- **Git:** 1 modified (.gitignore), ~20 untracked dirs (agents/, skills/, CLAUDE.md, scripts/, tools/)
- **Paperclip:** `localhost:3101`, postgres 54330, Company ID `1652ca87-e9d9-4ffe-9c32-f2785ea17c93`
- **npm:** `aiskillslab@0.1.0` published and working
- **All agents idle, no in-progress issues**

## Agents
| Agent | ID | Model | Status |
|---|---|---|---|
| Research | `4e882a53` | Sonnet | Calibrated, auto-hands off to SkillBuilder |
| SkillBuilder | `2dde30cb` | Sonnet | Calibrated, auto-hands off to QC |
| QC | `b2198547` | Haiku | Reviews skills, publishes on PASS, bounces on FAIL |
| CustomerRelations | `e623f014` | Sonnet | Email triage via Resend/Convex API |

## Skills Built (5 total, all published to Convex + available via CLI)
1. autonomous-agent — 6 Claude Code primitives
2. persistent-memory — file-based cross-session memory
3. multi-agent-coordination — parallel agents, worktrees, cost control
4. context-cost-management — token budgets, MCP slimming, model routing
5. git-workflow-automation — worktrees, gh CLI, branch-to-PR loops

## Key Infrastructure
- **CLI:** `~/projects/business/aiskillslab-cli/` → `npx aiskillslab install <skill>`
- **Publish script:** `scripts/publish-skill.ts` — pushes to Convex, QC calls on PASS
- **Convex API:** `/api/skills/list`, `/api/skills/get`, `/api/skills/publish`, `/api/mail/*`
- **AGENT_API_KEY:** set in Convex env for agent HTTP access
- **html2pptx:** `tools/html2pptx/` with templates and LibreOffice on PATH
- **Remotion:** `~/projects/business/video-pipeline/` with Code Hike template
- **YouTube:** banner + avatar at `tools/html2pptx/youtube-banner.png`
- **CLAUDE.md:** silent heartbeat loop (CEO monitoring)

## Gotchas Hit
- Paperclip two-step assign: must PATCH assignee and status separately
- Paperclip port shifted to 3101 (old process held 3100)
- npm publish requires interactive 2FA (can't automate from CLI)
- SkillBuilder handoff creates issues in `todo` not `in_progress` — two-step fix applied but untested

## Next Steps
1. **Test full pipeline with QC auto-publish** — fire Research, watch it flow through SkillBuilder → QC → publish to website automatically
2. **TutorialWriter agent** — next in the build order after QC is proven
3. **VideoProducer agent** — uses Remotion, follows TutorialWriter
4. **CR agent heartbeat** — wire to auto-check inbox instead of manual task creation
5. **Commit all untracked files** — lots of new content not in git
6. **QC tuning** — AIS-16 re-review didn't post a proper review comment, needs investigation
