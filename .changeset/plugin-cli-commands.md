---
"@paperclipai/cli": minor
"@paperclipai/server": minor
---

Add CLI commands for plugin management: `paperclipai plugin list`, `install`, `uninstall`, `enable`, `disable`, `get`, `doctor`, `create`, and `upgrade`. Mirrors the UI plugin manager for headless/automation use.

