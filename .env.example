DATABASE_URL=postgres://paperclip:CHANGE_ME@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
# Generate with: openssl rand -hex 32
BETTER_AUTH_SECRET=CHANGE_ME
# Must be separate from BETTER_AUTH_SECRET. Generate with: openssl rand -hex 32
PAPERCLIP_AGENT_JWT_SECRET=CHANGE_ME
