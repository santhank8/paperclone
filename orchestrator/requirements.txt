# Kaizai Workforce — Orchestrator dependencies
# TODO: replace with the canonical requirements.txt from
# stepan-korec/trading-agent:agent-workforce/requirements.txt during the
# code-extraction step (Phase 0B / Step 4 of the migration spec).
# This placeholder lists the dependencies the orchestrator is expected to
# need so the image can build before the real source is copied in.

# LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.2.0
langsmith>=0.1.0

# Anthropic SDK
anthropic>=0.39.0

# GitHub integration
PyGithub>=2.4.0
PyJWT[crypto]>=2.8.0

# Slack integration
slack-sdk>=3.27.0

# Persistence
psycopg[binary]>=3.2.0
SQLAlchemy>=2.0.0

# Config + utilities
pydantic>=2.8.0
pydantic-settings>=2.5.0
PyYAML>=6.0
httpx>=0.27.0
tenacity>=9.0.0
